# 通用配置（所有环境共享）
server.port=8091

# Mybatis配置
mybatis.mapper-locations=classpath*:com/xiaozhi/mapper/*.xml

# 数据库连接池通用配置
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.maximum-pool-size=100
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.pool-name=DatebookHikariCP
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.leak-detection-threshold=60000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.register-mbeans=true

# 日志通用配置
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.io.github.imfangs.dify.client.impl.StreamEventDispatcher=ERROR
logging.level.io.netty.handler=ERROR

# 禁用Groovy模板位置检查
spring.groovy.template.check-template-location=false

# 文件上传配置
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=2048MB
spring.servlet.multipart.max-request-size=2048MB

spring.jmx.enabled=false

# 允许的WebSocket源（跨域配置），多个源用逗号分隔
spring.websocket.allowed-origins=*

# 添加 Spring MVC Session 配置
spring.session.cookie.http-only=true
spring.session.cookie.secure=false
spring.session.cookie.same-site=lax
spring.session.cookie.path=/

# 启用虚拟线程
spring.threads.virtual.enabled=true

# SpringDoc OpenAPI 配置
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
# 启用 Swagger UI
springdoc.swagger-ui.enabled=true
# 确保静态资源映射正确
spring.web.resources.add-mappings=true
# 配置 Swagger UI 的 URL
springdoc.swagger-ui.url=/v3/api-docs

# 激活的配置文件（默认为开发环境）
spring.profiles.active=dev

# 是否定时关闭不活跃的会话（秒）
check.inactive.session=true
# MQTT 服务配置
xiaozhi.mqtt.enabled=true
xiaozhi.mqtt.host=0.0.0.0
xiaozhi.mqtt.port=1883
xiaozhi.mqtt.external-host=
xiaozhi.mqtt.username=gaoyang
xiaozhi.mqtt.password=alsdkfjg123jksdf123
xiaozhi.mqtt.topic-prefix=xiaozhi/device
xiaozhi.mqtt.keepalive=240
xiaozhi.mqtt.udp.port-start=20000
xiaozhi.mqtt.udp.port-end=21000

# OTA 配置（可被环境覆盖或在 docker 中通过环境变量注入）
# 自定义 OTA 固件下载地址；为空时将自动拼接为 http://{serverIp}:{server.port}/api/device/ota
xiaozhi.ota.url=
# 固件版本号
xiaozhi.firmware.version=2.1.1

# SSL配置
# 是否跳过SSL证书验证（仅用于开发环境，生产环境请设置为false）
xiaozhi.ssl.skip-verification=true
# SSL连接超时时间（秒）
xiaozhi.ssl.connect-timeout=30
xiaozhi.firmware.downloadurl=http://192.168.1.54:8091/api/file/firmware/download/firmware_1761054662289.bin

# 文件存储配置
# 文件上传存储路径（Docker环境中的绝对路径）
xiaozhi.file.upload-path=/app/files
# 文件下载URL前缀（可选，为空时使用当前服务器地址）
xiaozhi.file.download-url-prefix=
